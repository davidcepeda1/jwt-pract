# Server
PORT=3000

# JWT — asymmetric RS256 keys (paths relative to project root)
JWT_PRIVATE_KEY_PATH=./private.pem
JWT_PUBLIC_KEY_PATH=./public.pem

# Sentry — observability & error tracking
SENTRY_DSN=
SENTRY_ENVIRONMENT=
